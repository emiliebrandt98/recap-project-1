console.clear();

const body = document.querySelector("body");
const darkModeButton = document.querySelector('[data-js="dark-mode-toggle"]');

darkModeButton.addEventListener("click ", (event) => {});
